/**
 * Created by kagarwal on 8/12/14.
 */
'use strict';
module.exports = {
    area : function area(r){
        return Math.PI * r * r;
    },

    circumference : function circumference(r){
        return 2 * Math.PI * r;
    }
};